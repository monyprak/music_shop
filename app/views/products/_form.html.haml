
/ = form_tag(product_path(), method: "get") do
  
/   .span3 
/     //for image

/   .span9.pull-right 
/     = label_tag :name
/     = text_field_tag(:name, @product.name, class: "textbox-form-width")
    
/     = label_tag :price
/     = text_field_tag(:price, @product.price, class: "textbox-form-width")
    
/     = label_tag :description
/     = text_area_tag(:description, @product.description, class: "textarea-form")
    
/     = label_tag :category
/     = select_tag :category, options_from_collection_for_select(@categories, "id", "name", @product.category_id), prompt: "Please select category", class: "select-form-width dropdown-toggles"

/     = label_tag :brand
/     = select_tag :brand, options_from_collection_for_select(@brands, "id", "name", @product.brand_id), prompt: "Please select brand", class: "select-form-width dropdown-toggles"

/     = label_tag :top_product
/     = check_box_tag :top_product, @product.top_product


/   .span12.footer-form
/     = submit_tag 'Save', class: "btn btn-success button-spacing"  
/     = link_to 'Cancel' , products_path() , class: "btn"   


= form_for(@product, html: {multipart: true}) do |f|
  .field
    .span3
      // for img

    .span9.pull-right  
      = f.label :name
      = f.text_field :name, class: "textbox-form-width"
    
      = f.label :price
      = f.text_field :price, class: "textbox-form-width"
    
      = f.label :description
      = f.text_area :description, class: "textarea-form"
    
      = f.label :path_img
      = f.file_field :path_img
    

      = f.label :category
      = f.select :category_id, options_from_collection_for_select(@categories, "id", "name", @product.category_id), prompt: "Please select category", class: "select-form-width"

      = f.label :brand
      = f.select :brand_id, options_from_collection_for_select(@brands, "id", "name", @product.brand_id), prompt: "Please select category", class: "select-form-width"

      = f.label :top_product
      = f.check_box :top_product

    .span12.footer-form  
      = f.submit "Save" , class: "btn btn-success button-spacing"
      = link_to 'Cancel' , products_path() , class: "btn"